steamlocomotiveinsurance.com — BUILD STATUS
======================================
Niche: Heritage steam railroad operators — steam excursion railroads, steam
       locomotive restoration shops, railroad museums running steam, and dinner/
       tourist steam trains. Framed around the steam-railroad operator.
       (Converted from a raw framing clone left by an interrupted prior task —
        grep fram was 131; now 0.)

Build: GREEN — `next build` exit code 0 (Next.js 15.5.x)
  .next/BUILD_ID present = true (gLfvhxiW0_X7KsZFvyBBt)
  Built atomically under the shared flock (pnpm install ; next build — `;` used so
  pnpm's ERR_PNPM_IGNORED_BUILDS sharp advisory exit-1 doesn't skip the build).
Routes: 24 prerendered pages
  - Static: /, /about, /blog, /contact, /privacy, /quote, /services, /terms,
    /robots.txt, /sitemap.xml, /_not-found
  - Services (7, SSG): general-liability-insurance, railroad-liability-insurance,
    passenger-liability-insurance, rolling-stock-equipment-insurance,
    boiler-machinery-insurance, commercial-property-insurance,
    workers-compensation-insurance
  - Blog (4, SSG): steam-railroad-insurance-guide,
    steam-boiler-machinery-insurance-explained,
    railroad-liability-fela-steam-operators,
    passenger-liability-steam-excursion-trains

Images: 9 real FLUX images in public/images/, all > 10KB
  hero-steam, boiler-closeup, footplate-crew, restoration-shop, steam-excursion,
  steam-roundhouse, about-agent, blog-default, og-image
  (6 leftover framing images deleted.)

Forms: 2 Netlify Forms — contact (name="contact") and quote (name="quote").
  Each has data-netlify="true", data-netlify-honeypot="bot-field",
  hidden form-name input, and the bot-field honeypot. Both submit via
  fetch("/") with URLSearchParams (Netlify pattern) — the quote page's original
  direct webhook fetch (site=framinginsurance.com) was REMOVED (no WEBHOOK_URL).
  netlify.toml notifications route both forms to the josh tenant webhook with
  site=steamlocomotiveinsurance.com.

SEO: sitemap.ts, robots.ts, and public/llms.txt all updated to
  https://steamlocomotiveinsurance.com with heritage steam railroad keywords.

Contact (canonical): Phone 844-967-5247 (tel:+18449675247),
  Email josh@contractorschoiceagency.com, HQ 12220 E Riggs Rd, Chandler, AZ 85249.

Testimonials: REMOVED — TESTIMONIALS array deleted from site.ts, Testimonials
  section removed from homepage, Testimonials.tsx component deleted.

Build config: inherited proven setup left intact — netlify.toml command
  "pnpm run build", NODE_VERSION 20, pnpm-lock.yaml only, NO packageManager field.

Framing scrub: `grep -rin 'fram' src/` returns ZERO matches except the JS API
  requestAnimationFrame (SmoothScroll.tsx). Clean, including the quote page.
  `grep -rn "WEBHOOK_URL|fetch(WEBHOOK" src/` = 0. No framinginsurance anywhere
  in src/public/netlify.toml/package.json.
